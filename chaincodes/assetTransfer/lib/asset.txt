const CV {
ID:
FirstName: First Name
LastName: LastName
Jobtitle: JobtitleVal
PreviousJobList: PreviousJobs
EducationList: Education

}


List PreviousJobs [job1, job2, ..]

const Job {
	StartDate
	EndDate
	Title
}

List Education[education1, education2, ...]

const educations{
	StartYear
	EndYear
	Title
	Grade
}


sudo ./networkdown.sh
sudo ./network.sh up createChannels
sudo ./network.sh deployCCs
sudo ./network.sh deployCCs -n assetTransfer
sudo ./network.sh invokeCC